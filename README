# IlluConv - An Illustrative Number System Converter

## Quickstart
```
usage: illuconv.py [-h] [-b] [-d] input_number

positional arguments:
  input_number      User-provided integer to convert

options:
  -h, --help        show this help message and exit
  -b, --to_binary   Convert input to binary
  -d, --to_decimal  Convert input to decimal
```